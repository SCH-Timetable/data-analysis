# MySQL connection used by the FastAPI backend
DATABASE_URL=mysql+pymysql://bua_user:change_me@127.0.0.1:3306/bua_project
# Optional: Gemini API key for chatbot NLU/NLG polish.
# If empty, chatbot still works with the deterministic Arabic fallback.
GEMINI_API_KEY=
